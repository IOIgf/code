拓扑排序板子题。

甚至连高精都不用，用 `__int128` 就能创过去。

就是通分有点麻烦。注意要初始化所有结点的分母为 $1$，防止爆炸。

$fz_i$ 记录每个结点的分子，$fm_i$ 记录每个结点的分母。

拓扑排序时，对于每一个结点，其流量为当前流量加上上一个结点的流量除以上一个结点的流出管道数量。

通分原理：
$$\frac{a}{b}+\frac{c}{d}=\frac{ad+bc}{bd}$$