## 题意简述

给定一个无向图，判断能否通过删除 $k$ 个点使图不连通，并输出这些点。

## 思路

首先需要判断原图是否连通，不连通接输出 `Poor SOL!`。

注意到 $k \le 3$，所以考虑分类讨论。

当 $k=1$ 时，直接跑 $Tarjan$，输出任意一个割点，即可使图不连通。

如果没有割点，则输出 `How oversuspicious you are, SOL!`。

当 $k=2$ 时，枚举每一个点，给它打上标记，当做把它删除，再跑 $Tarjan$，当搜索到标记的点时，直接跳过它就行。